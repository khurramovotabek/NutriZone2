# Intentionally empty. DJANGO_SETTINGS_MODULE should point directly at
# config.settings.development or config.settings.production (see manage.py,
# config/wsgi.py, config/asgi.py, and .env.example).
